<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.freework.cvitae.dao.EnterpriseCvDao">

    <select id="queryByRequirement" parameterType="com.freework.cvitae.entity.EnterpriseCv"
            resultType="com.freework.cvitae.entity.EnterpriseCv">
        SELECT
        enterprise_cv_id,
        enterprise_id,
        vocation_id,
        status,
        user_id,
        curriculum_vitae_id,
        address,
        create_time,
        last_edit_time
        FROM
        tb_enterprise_cv
        <where>
            <if test="userId != null">
                AND user_id=#{userId}
            </if>
            <if test="status == 2">
                AND (status = 0 OR status = 1)
            </if>
            <if test="status != null and status != 2">
                AND status = #{status}
            </if>
            <if test="enterpriseId != null">
                AND enterprise_id=#{enterpriseId}
            </if>
            <if test="vocationId != null">
                AND vocation_id=#{vocationId}
            </if>
        </where>
        ORDER BY create_time DESC
    </select>

    <insert id="insert" useGeneratedKeys="true"
            keyProperty="enterpriseCvId" keyColumn="enterpriseCvId">
        INSERT INTO
        tb_enterprise_cv(
        enterprise_id,
        vocation_id,
        curriculum_vitae_id,
        status,
        address,
        create_time,
        last_edit_time
        ) VALUES (
        #{enterpriseId},
        #{vocationId},
        #{curriculumVitaeId},
        #{status},
        #{address},
        #{createTime},
        #{lastEditTime}
        )
    </insert>

    <update id="update" parameterType="com.freework.cvitae.entity.EnterpriseCv">
        UPDATE
        tb_enterprise_cv
        SET
        status=#{status},
        last_edit_time=#{lastEditTime}
        WHERE
        enterprise_cv_id=#{enterpriseCvId}
        AND enterprise_id=#{enterpriseId}
    </update>
</mapper>