<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.freework.cvitae.dao.EnterpriseCvDao">
    <select id="queryByUserId" parameterType="integer">
        SELECT
        enterprise_cv_id, enterprise_id, curriculum_vitae_id, status, create_time, last_edit_time
        FROM
        tb_enterprise_cv,
        tb_curriculum_vitae
        WHERE
        tb_enterprise_cv.curriculum_vitae_id = tb_curriculum_vitae.curriculum_vitae_id
        AND tb_curriculum_vitae.user_id=#{userId}
    </select>
</mapper>